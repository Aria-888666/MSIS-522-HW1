# MSIS 522 — HW1: Orange Juice Demand Prediction

**Course:** MSIS 522 — Analytics and Machine Learning  
**School:** Foster School of Business, University of Washington  
**Assignment:** Homework 1 — The Complete Data Science Workflow  

---

## 🔗 Live Streamlit App

 **[https://msis-522-hw1-n6z6tdadib5xgjibjxpdvc.streamlit.app](https://msis-522-hw1-n6z6tdadib5xgjibjxpdvc.streamlit.app)**

---

## Project Overview

This project implements the full end-to-end data science workflow on the
**Dominick's Finer Foods Orange Juice dataset** — a classic retail analytics dataset
containing weekly store-level observations of orange juice sales across multiple locations.

**Prediction task:** Regression — predict `logmove` (log of weekly units sold)  
**Dataset:** ~28,000 weekly store-level observations  
**Features:** Price, promotions (`feat`), brand, and store demographic variables
(income, age, etc.)

---

## Repository Structure

```
MSIS-522-HW1/
├── Streamlit_app.py       # Main Streamlit application (all 4 tabs)
├── train_models.py        # Train and save all 7 models with GridSearchCV
├── preprocessing.py       # Data loading and sklearn preprocessing pipeline
├── shap_analysis.py       # Standalone SHAP analysis and plot generation
├── oj.csv                 # Orange juice dataset
├── model_results.csv      # Test-set metrics for all models (generated by train_models.py)
├── models/                # Saved model .pkl files (generated by train_models.py)
│   ├── linear.pkl
│   ├── ridge.pkl
│   ├── lasso.pkl
│   ├── cart.pkl
│   ├── random_forest.pkl
│   ├── lightgbm.pkl
│   └── mlp.pkl
├── requirements.txt       # Python dependencies with pinned versions
└── README.md              # This file
```

---

## How to Run Locally

### 1. Clone the repository

```bash
git clone https://github.com/Aria-888666/MSIS-522-HW1.git
cd MSIS-522-HW1
```

### 2. Install dependencies

```bash
pip install -r requirements.txt
```

> **Important:** The `scikit-learn` version in `requirements.txt` must match the version
> used to train the saved `.pkl` models. If you retrain locally, update `requirements.txt`
> with the exact version printed by `python train_models.py`.

### 3. Train all models

```bash
python train_models.py
```

This will:
- Load `oj.csv` and run the full preprocessing pipeline
- Train 7 models with 5-fold GridSearchCV tuning (Decision Tree, Random Forest, LightGBM)
- Save each model to `models/<model_name>.pkl`
- Save test-set metrics to `model_results.csv`
- Print best hyperparameters and metrics to the console

### 4. Run SHAP analysis

```bash
python shap_analysis.py
```

Generates and saves `shap_summary.png`, `shap_bar.png`, and `shap_waterfall.png`.

### 5. Launch the Streamlit app

```bash
streamlit run Streamlit_app.py
```

Then open [http://localhost:8501](http://localhost:8501) in your browser.

---

## Models Trained

| Model | Hyperparameters Tuned (GridSearchCV) |
|---|---|
| Linear Regression | — (baseline) |
| Ridge Regression | `alpha` |
| Lasso Regression | `alpha` |
| Decision Tree (CART) | `max_depth`, `min_samples_leaf` |
| Random Forest | `n_estimators`, `max_depth` |
| LightGBM | `n_estimators`, `learning_rate`, `max_depth` |
| Neural Network (MLP) | `hidden_layer_sizes`, `max_iter` |

All models use `random_state=42`. Cross-validation uses 5 folds, scoring on
`neg_mean_squared_error`.

---

## Streamlit App — Tab Overview

| Tab | Contents |
|---|---|
| Executive Summary | Dataset description, business context, approach, key findings |
| Descriptive Analytics | Target distribution, 4+ feature visualizations, correlation heatmap |
| Model Performance | Results table, RMSE bar chart, predicted vs actual plots, decision tree viz, MLP loss curve, hyperparameter table |
| Explainability & Prediction | SHAP beeswarm, bar plot, waterfall; interactive prediction with price/promotion/brand sliders; per-input SHAP waterfall |

> **Note:** The Streamlit app trains fresh models at runtime to avoid sklearn version
> compatibility issues with the saved `.pkl` files. The `.pkl` files are used only when
> running `shap_analysis.py` or other offline scripts.

---

## Requirements

Key dependencies (see `requirements.txt` for full pinned versions):

```
streamlit
pandas
numpy
scikit-learn==X.X.X      # must match training environment
lightgbm
shap
matplotlib
seaborn
joblib
```
